export default function ({ name, description, version }) {
    return {
        name: `${name}`,
        description: `${description}`,
        version: `${varsion}`
        "dependencies": {
            "react": "^16.8.6",
            "react-dom": "^16.8.6"
        },
        "devDependencies": {}
    }
}
