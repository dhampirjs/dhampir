export default function ({ name, description, version }) {
    return {
        name: `${name}`,
        description: `${description}`,
        version: `${version}`,
        dependencies: {
        },
        devDependencies: {}
    }
}
